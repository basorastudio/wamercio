'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
import {api} from '@/lib/api'
import {Eye,EyeOff,LockKeyhole,ShieldCheck} from 'lucide-react'
export default function AdminLogin(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api('/auth/admin/login',{method:'POST',body:JSON.stringify({email,password})});router.replace('/admin')}catch(e:any){setError(e.message)}finally{setBusy(false)}};return <main className="grid min-h-dvh bg-slate-950 lg:grid-cols-[1.05fr_.95fr]"><section className="hidden flex-col justify-between p-12 text-white lg:flex"><div className="flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-amber-400 text-xl font-black text-slate-950">W</div><div><div className="text-2xl font-black">WAMERCIO</div><div className="text-xs font-bold uppercase tracking-[.18em] text-amber-300">Administración SaaS</div></div></div><div className="max-w-xl"><span className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm text-amber-300"><ShieldCheck className="h-4 w-4"/>Acceso restringido</span><h1 className="mt-6 text-5xl font-black leading-tight">Control central de la plataforma.</h1><p className="mt-5 max-w-lg text-lg leading-8 text-slate-400">Usuarios, tiendas, planes, suscripciones, soporte y métricas globales en un panel totalmente separado de la operación comercial.</p></div><p className="text-sm text-slate-600">WAMERCIO · SuperAdmin</p></section><section className="flex items-center justify-center bg-white p-5 sm:p-8"><form onSubmit={submit} className="w-full max-w-md"><div className="mb-8 lg:hidden"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-slate-950 font-black text-amber-300">W</span><div><div className="text-xl font-black">WAMERCIO</div><div className="text-xs font-bold uppercase tracking-wider text-slate-500">SuperAdmin SaaS</div></div></div></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-slate-100 text-slate-700"><LockKeyhole className="h-6 w-6"/></div><p className="mt-5 text-xs font-bold uppercase tracking-[.16em] text-amber-600">Administración central</p><h2 className="mt-1 text-3xl font-black text-slate-950">Iniciar sesión</h2><p className="mt-2 text-sm leading-6 text-slate-500">Este acceso es exclusivo para el administrador SaaS.</p>{error&&<div className="mt-5 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<div className="mt-7"><label className="label">Correo administrativo</label><input className="field h-12" type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@wamercio.com"/></div><div className="mt-4"><label className="label">Contraseña</label><div className="relative"><input className="field h-12 pr-12" type={show?'text':'password'} autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div></div><button disabled={busy} className="mt-6 h-12 w-full rounded-xl bg-slate-950 px-4 text-sm font-bold text-white transition hover:bg-slate-800 disabled:opacity-60">{busy?'Validando...':'Entrar al SuperAdmin'}</button></form></section></main>}
