'use client'
import {useEffect,useMemo,useState} from 'react'
import AdminShell from '@/components/admin-shell'
import {api,dateTime} from '@/lib/api'
import {Loading,SearchBox,Status} from '@/components/ui'
import {ExternalLink} from 'lucide-react'
export default function AdminStores(){const[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState('');useEffect(()=>{api<any[]>('/admin/stores').then(setRows).finally(()=>setLoading(false))},[]);const filtered=useMemo(()=>rows.filter(x=>(x.name+' '+x.slug+' '+x.owner+' '+x.owner_email).toLowerCase().includes(search.toLowerCase())),[rows,search]);return <AdminShell title="Tiendas SaaS" subtitle="Todas las tiendas creadas dentro de WAMERCIO"><div className="mb-5"><SearchBox value={search} onChange={setSearch} placeholder="Buscar tienda o propietario..."/></div>{loading?<Loading/>:<div className="table-wrap overflow-x-auto"><table className="table"><thead><tr><th>Tienda</th><th>Propietario</th><th>Productos</th><th>Pedidos</th><th>Estado</th><th>Creada</th><th></th></tr></thead><tbody>{filtered.map(s=><tr key={s.id}><td><div className="font-semibold">{s.name}</div><div className="text-xs text-slate-400">/{s.slug}</div></td><td><div>{s.owner}</div><div className="text-xs text-slate-400">{s.owner_email}</div></td><td>{s.products}</td><td>{s.orders}</td><td><Status value={s.is_active?'active':'inactive'}/></td><td className="text-slate-500">{dateTime(s.created_at)}</td><td><a target="_blank" href={`/store/${s.slug}`} className="btn-secondary px-3"><ExternalLink className="h-4 w-4"/></a></td></tr>)}</tbody></table></div>}</AdminShell>}
