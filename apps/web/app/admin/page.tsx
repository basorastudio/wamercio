'use client'
import {useEffect,useState} from 'react'
import Link from 'next/link'
import SuperAdminShell from '@/components/superadmin-shell'
import {api,money} from '@/lib/api'
import {Loading} from '@/components/ui'
import {UsersRound,Store,Boxes,ShoppingBag,CircleDollarSign,Clock3,ArrowUpRight,LifeBuoy} from 'lucide-react'
export default function AdminDashboard(){const[d,setD]=useState<any>(null);useEffect(()=>{api('/admin/dashboard').then(setD)},[]);return <SuperAdminShell title="Superadministración de WAMERCIO" subtitle="Resumen central de la plataforma">{!d?<Loading/>:<><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-7">{[['Propietarios',d.users,UsersRound],['Negocios',d.stores,Store],['Productos',d.products,Boxes],['Pedidos',d.orders,ShoppingBag],['Ventas',money(d.revenue),CircleDollarSign],['Solicitudes',d.pending_plan_requests,Clock3],['Tickets',d.open_tickets,LifeBuoy]].map(([l,v,I]:any)=><div key={l} className="card p-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-brand-50 text-brand-600"><I className="h-5 w-5"/></div><div className="mt-4 text-sm font-medium text-[#8d92aa]">{l}</div><div className="mt-1 text-2xl font-semibold text-ink-900">{v}</div></div>)}</div><div className="mt-6 grid gap-5 lg:grid-cols-4">{[['/admin/owners','Propietarios','Gestiona propietarios y negocios desde una sola sección.'],['/admin/landing','Página comercial','Personaliza la página principal y su modo de mantenimiento.'],['/admin/plans','Planes y suscripciones','Administra la oferta comercial y las solicitudes de cambio.'],['/admin/settings','Centro SaaS','Centraliza estructura, integraciones y políticas de plataforma.']].map(([href,t,d])=><Link key={href} href={href} className="card p-5 transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start"><div><h3 className="font-bold">{t}</h3><p className="mt-2 text-sm leading-6 text-[#8d92aa]">{d}</p></div><ArrowUpRight className="ml-auto h-5 w-5 text-brand-600"/></div></Link>)}</div></>}</SuperAdminShell>}
