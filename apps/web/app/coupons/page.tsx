'use client'
import {useEffect,useState} from 'react'
import AdminShell,{StoreSelector} from '@/components/store-shell'
import {api,money} from '@/lib/api'
import {Alert,Loading,Modal,PageEmpty,Status} from '@/components/ui'
import type {Coupon} from '@/lib/types'
import {Plus,Pencil,Trash2,TicketPercent} from 'lucide-react'
const blank={code:'',discount_type:'percentage',discount_value:10,min_order:0,usage_limit:null,is_active:true}
export default function Coupons(){const[store,setStore]=useState(''),[rows,setRows]=useState<Coupon[]>([]),[loading,setLoading]=useState(false),[open,setOpen]=useState(false),[edit,setEdit]=useState<Coupon|null>(null),[form,setForm]=useState<any>(blank),[err,setErr]=useState('');const load=()=>{if(!store)return;setLoading(true);api<Coupon[]>(`/coupons?store_id=${store}`).then(setRows).finally(()=>setLoading(false))};useEffect(load,[store]);const start=(x?:Coupon)=>{setEdit(x||null);setForm(x?{...x}:blank);setErr('');setOpen(true)};const save=async(e:React.FormEvent)=>{e.preventDefault();try{const body={...form,store_id:store};if(edit)await api(`/coupons/${edit.id}`,{method:'PUT',body:JSON.stringify(body)});else await api('/coupons',{method:'POST',body:JSON.stringify(body)});setOpen(false);load()}catch(e:any){setErr(e.message)}};const del=async(x:Coupon)=>{if(!confirm(`¿Eliminar cupón ${x.code}?`))return;await api(`/coupons/${x.id}`,{method:'DELETE'});load()};return <AdminShell title="Cupones" subtitle="Promociones de monto fijo o porcentaje" context={<StoreSelector value={store} onChange={setStore}/>} actions={<button disabled={!store} onClick={()=>start()} className="btn-primary"><Plus className="h-4 w-4"/> Nuevo cupón</button>}>{!store?<PageEmpty title="Selecciona una tienda" detail="Elige una tienda para administrar sus cupones."/>:loading?<Loading/>:rows.length===0?<PageEmpty title="No hay cupones" detail="Crea una promoción para incentivar nuevas compras."/>:<div className="table-wrap overflow-x-auto"><table className="table"><thead><tr><th>Código</th><th>Descuento</th><th>Mínimo</th><th>Usos</th><th>Estado</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><span className="inline-flex items-center gap-2 rounded-lg bg-brand-50 px-3 py-1.5 font-mono font-bold text-brand-700"><TicketPercent className="h-4 w-4"/>{x.code}</span></td><td className="font-semibold">{x.discount_type==='percentage'?`${x.discount_value}%`:money(x.discount_value)}</td><td>{money(x.min_order)}</td><td>{x.used_count}{x.usage_limit?` / ${x.usage_limit}`:''}</td><td><Status value={x.is_active?'active':'inactive'}/></td><td><div className="flex justify-end gap-2"><button className="btn-secondary px-3" onClick={()=>start(x)}><Pencil className="h-4 w-4"/></button><button className="btn-danger px-3" onClick={()=>del(x)}><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div>}<Modal open={open} onClose={()=>setOpen(false)} title={edit?'Editar cupón':'Nuevo cupón'}><form onSubmit={save}>{err&&<Alert text={err}/>}<div className="space-y-4"><div><label className="label">Código *</label><input className="field uppercase" required value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})} placeholder="WAMERCIO10"/></div><div className="grid grid-cols-2 gap-3"><div><label className="label">Tipo</label><select className="field" value={form.discount_type} onChange={e=>setForm({...form,discount_type:e.target.value})}><option value="percentage">Porcentaje</option><option value="flat">Monto fijo</option></select></div><div><label className="label">Valor</label><input className="field" type="number" min="0" step="0.01" value={form.discount_value} onChange={e=>setForm({...form,discount_value:Number(e.target.value)})}/></div></div><div><label className="label">Compra mínima</label><input className="field" type="number" min="0" step="0.01" value={form.min_order} onChange={e=>setForm({...form,min_order:Number(e.target.value)})}/></div><div><label className="label">Límite de usos</label><input className="field" type="number" min="1" value={form.usage_limit??''} onChange={e=>setForm({...form,usage_limit:e.target.value===''?null:Number(e.target.value)})} placeholder="Sin límite"/></div>{edit&&<label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Activo</label>}</div><div className="mt-6 flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setOpen(false)}>Cancelar</button><button className="btn-primary">Guardar</button></div></form></Modal></AdminShell>}
