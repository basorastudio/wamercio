'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { api } from '@/lib/api'
import { Eye, EyeOff, MessageCircleMore } from 'lucide-react'
export default function Login(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});r.replace('/dashboard')}catch(e:any){setError(e.message)}finally{setBusy(false)}};return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-slate-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-brand-500 text-2xl font-black">W</div><div><div className="text-2xl font-extrabold">WAMERCIO</div><div className="text-xs uppercase tracking-[.2em] text-brand-300">Comercio conversacional</div></div></div><div className="max-w-xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm text-brand-200"><MessageCircleMore className="h-4 w-4"/> WhatsApp + Catálogo + Pedidos</div><h1 className="text-5xl font-black leading-tight">Convierte conversaciones en ventas.</h1><p className="mt-5 text-lg leading-8 text-slate-300">Administra tiendas, productos, pedidos y delivery desde una plataforma creada para negocios dominicanos.</p></div><p className="text-sm text-slate-500">WAMERCIO · República Dominicana</p></section><section className="flex items-center justify-center bg-white p-6"><form onSubmit={submit} className="w-full max-w-md"><div className="mb-8 lg:hidden"><div className="text-3xl font-black text-slate-900">WAMERCIO</div><div className="text-sm text-brand-600">Comercio conversacional</div></div><p className="text-sm font-semibold text-brand-600">Panel de gestión</p><h2 className="mt-1 text-3xl font-black text-slate-900">Inicia sesión</h2><p className="mt-2 text-slate-500">Accede a tu operación comercial.</p>{error&&<div className="mt-5 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<div className="mt-7"><label className="label">Correo electrónico</label><input className="field" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="tu@negocio.com" required/></div><div className="mt-4"><label className="label">Contraseña</label><div className="relative"><input className="field pr-12" type={show?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div></div><button disabled={busy} className="btn-primary mt-6 w-full">{busy?'Entrando...':'Entrar a WAMERCIO'}</button><p className="mt-5 text-center text-sm text-slate-500">¿Aún no tienes cuenta? <a href="/register" className="font-semibold text-brand-600">Crear cuenta</a></p></form></section></main>}
