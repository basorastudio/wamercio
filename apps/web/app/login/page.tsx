"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Store} from "lucide-react";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[err,setErr]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setErr("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setErr(d.error||"No fue posible iniciar sesión");return}router.push(d.user.role==="superadmin"?"/admin":d.user.role==="affiliate"?"/afiliado":"/panel")};return <main className="grid min-h-screen place-items-center bg-[#f5f5f5] px-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><div className="mb-8 flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[#ff5400] text-white"><Store/></div><div><div className="text-xl font-black">wamercio</div><div className="text-sm text-gray-500">Acceso administrativo</div></div></div><label className="text-sm font-bold">Correo</label><input className="field mt-2 mb-4" value={email} onChange={e=>setEmail(e.target.value)}/><label className="text-sm font-bold">Contraseña</label><input className="field mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<p className="mt-3 text-sm text-red-600">{err}</p>}<button className="btn btn-brand mt-6 w-full">Iniciar sesión</button><p className="mt-5 text-center text-xs text-gray-400">Usa las credenciales configuradas para tu cuenta.</p></form></main>}
