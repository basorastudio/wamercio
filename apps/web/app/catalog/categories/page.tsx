'use client'
import { useEffect,useState } from 'react'
import AdminShell,{StoreSelector} from '@/components/admin-shell'
import {api} from '@/lib/api'
import {Alert,Loading,Modal,PageEmpty,Status} from '@/components/ui'
import type {Category} from '@/lib/types'
import {Plus,Pencil,Trash2,Tags} from 'lucide-react'
const blank={name:'',slug:'',description:'',image_url:'',sort_order:0,is_active:true}
export default function Categories(){const [store,setStore]=useState(''),[rows,setRows]=useState<Category[]>([]),[loading,setLoading]=useState(false),[open,setOpen]=useState(false),[edit,setEdit]=useState<any>(null),[form,setForm]=useState<any>(blank),[err,setErr]=useState('');const load=()=>{if(!store){setRows([]);return};setLoading(true);api<Category[]>(`/categories?store_id=${store}`).then(setRows).finally(()=>setLoading(false))};useEffect(load,[store]);const start=(x?:Category)=>{setEdit(x||null);setForm(x?{...x}:blank);setErr('');setOpen(true)};const save=async(e:React.FormEvent)=>{e.preventDefault();try{const body={...form,store_id:store};if(edit)await api(`/categories/${edit.id}`,{method:'PUT',body:JSON.stringify(body)});else await api('/categories',{method:'POST',body:JSON.stringify(body)});setOpen(false);load()}catch(e:any){setErr(e.message)}};const del=async(x:Category)=>{if(!confirm(`¿Eliminar ${x.name}?`))return;await api(`/categories/${x.id}`,{method:'DELETE'}).catch((e)=>alert(e.message));load()};return <AdminShell title="Categorías" subtitle="Organiza el catálogo como en Foody Friend, con una experiencia más limpia" actions={<button disabled={!store} onClick={()=>start()} className="btn-primary"><Plus className="h-4 w-4"/> Nueva categoría</button>}><div className="mb-5 flex items-center justify-between"><StoreSelector value={store} onChange={setStore}/></div>{!store?<PageEmpty title="Selecciona una tienda" detail="El catálogo se administra de forma independiente para cada tienda."/>:loading?<Loading/>:rows.length===0?<PageEmpty title="No hay categorías" detail="Crea categorías para organizar los productos de esta tienda." action={<button className="btn-primary" onClick={()=>start()}>Crear categoría</button>}/>:<div className="table-wrap overflow-x-auto"><table className="table"><thead><tr><th>Categoría</th><th>Descripción</th><th>Orden</th><th>Estado</th><th className="text-right">Acciones</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center overflow-hidden rounded-xl bg-slate-100">{x.image_url?<img src={x.image_url} className="h-full w-full object-cover"/>:<Tags className="h-4 w-4 text-slate-400"/>}</div><div><div className="font-semibold text-slate-900">{x.name}</div><div className="text-xs text-slate-400">/{x.slug}</div></div></div></td><td className="max-w-md text-slate-500">{x.description||'—'}</td><td>{x.sort_order}</td><td><Status value={x.is_active?'active':'inactive'}/></td><td><div className="flex justify-end gap-2"><button onClick={()=>start(x)} className="btn-secondary px-3"><Pencil className="h-4 w-4"/></button><button onClick={()=>del(x)} className="btn-danger px-3"><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div>}<Modal open={open} onClose={()=>setOpen(false)} title={edit?'Editar categoría':'Nueva categoría'}><form onSubmit={save}>{err&&<Alert text={err}/>}<div className="space-y-4"><div><label className="label">Nombre *</label><input className="field" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label className="label">Identificador</label><input className="field" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} placeholder="se genera automáticamente"/></div><div><label className="label">Descripción</label><textarea className="field min-h-24" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div><label className="label">URL de imagen</label><input className="field" value={form.image_url} onChange={e=>setForm({...form,image_url:e.target.value})}/></div><div><label className="label">Orden</label><input className="field" type="number" value={form.sort_order} onChange={e=>setForm({...form,sort_order:Number(e.target.value)})}/></div>{edit&&<label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Activa</label>}</div><div className="mt-6 flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setOpen(false)}>Cancelar</button><button className="btn-primary">Guardar</button></div></form></Modal></AdminShell>}
