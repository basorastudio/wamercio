'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
import {api} from '@/lib/api'
import {Store,MessageCircleMore,CheckCircle2} from 'lucide-react'
export default function Register(){const r=useRouter();const[form,setForm]=useState({name:'',email:'',phone:'',password:''});const[err,setErr]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setErr('');try{await api('/auth/register',{method:'POST',body:JSON.stringify(form)});r.replace('/dashboard')}catch(e:any){setErr(e.message)}finally{setBusy(false)}};return <main className="grid min-h-screen bg-white lg:grid-cols-[1fr_1.1fr]"><section className="flex items-center justify-center p-6 sm:p-10"><form onSubmit={submit} className="w-full max-w-lg"><a href="/login" className="mb-8 inline-flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-brand-600 text-xl font-black text-white">W</span><span className="text-xl font-extrabold">WAMERCIO</span></a><p className="text-sm font-semibold text-brand-600">Empieza gratis</p><h1 className="mt-1 text-3xl font-black text-slate-900">Crea tu cuenta</h1><p className="mt-2 text-slate-500">Incluye una tienda, hasta 50 productos y conexión WhatsApp.</p>{err&&<div className="mt-5 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{err}</div>}<div className="mt-7 grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><label className="label">Nombre o responsable *</label><input className="field" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="sm:col-span-2"><label className="label">Correo *</label><input className="field" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="sm:col-span-2"><label className="label">WhatsApp / teléfono</label><input className="field" inputMode="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div className="sm:col-span-2"><label className="label">Contraseña *</label><input className="field" type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><p className="mt-1 text-xs text-slate-400">Mínimo 8 caracteres.</p></div></div><button disabled={busy} className="btn-primary mt-6 w-full">{busy?'Creando cuenta...':'Crear mi cuenta'}</button><p className="mt-5 text-center text-sm text-slate-500">¿Ya tienes cuenta? <a href="/login" className="font-semibold text-brand-600">Iniciar sesión</a></p></form></section><section className="hidden bg-slate-950 p-12 text-white lg:flex lg:flex-col lg:justify-center"><div className="max-w-xl"><span className="inline-flex items-center gap-2 rounded-full bg-white/5 px-4 py-2 text-sm text-brand-300"><MessageCircleMore className="h-4 w-4"/> WhatsApp-first</span><h2 className="mt-6 text-5xl font-black leading-tight">Tu comercio digital, sin depender de Twilio.</h2><p className="mt-5 text-lg leading-8 text-slate-300">Catálogo, carrito, pedidos y conexión directa de WhatsApp en una sola plataforma.</p><div className="mt-8 space-y-4">{['Catálogo público para cada tienda','Variantes, extras, cupones y delivery','Pedidos con actualización de estados','WhatsApp mediante QR con motor propio'].map(x=><div key={x} className="flex items-center gap-3 text-slate-200"><CheckCircle2 className="h-5 w-5 text-brand-400"/>{x}</div>)}</div></div></section></main>}
