'use client'

import {useEffect,useMemo,useState} from 'react'
import StoreShell,{StoreSelector} from '@/components/store-shell'
import {api} from '@/lib/api'
import {Alert,Loading,Modal,PageEmpty} from '@/components/ui'
import {Clock3,Copy,Download,ExternalLink,Plus,QrCode,Save,Trash2,UsersRound,UtensilsCrossed,Palette} from 'lucide-react'
import {QRCodeSVG} from 'qrcode.react'
import {resolveBusinessCapabilities} from '@/lib/business-capabilities'

type TableArea={id:string;name:string;sort_order:number;is_active:boolean;table_count:number;capacity:number;created_at?:string}
type TableRow={id:string;area_id:string;area_name:string;name:string;capacity:number;sort_order:number;is_active:boolean;created_at?:string}
type QRStyle={foreground:string;background:string;show_logo:boolean;label:string;frame_style:'rounded'|'minimal'|'card';is_override?:boolean;table_id?:string}
type BulkTableDraft={area_id:string;base_name:string;suffix_type:'numeric'|'alphabetic'|'mixed';quantity:number;capacity:number}
const alphabeticTableSuffix=(value:number)=>{let n=Math.max(1,Math.floor(value)),out='';while(n>0){n--;out=String.fromCharCode(65+(n%26))+out;n=Math.floor(n/26)}return out}
const generatedTableName=(base:string,suffix:'numeric'|'alphabetic'|'mixed',index:number)=>`${base.trim()} ${suffix==='alphabetic'?alphabeticTableSuffix(index):suffix==='mixed'?`A${index}`:index}`.trim()
const defaultQRStyle:QRStyle={foreground:'#111827',background:'#FFFFFF',show_logo:true,label:'Escanea para ordenar',frame_style:'rounded'}

export default function TablesPage(){
 const[store,setStore]=useState('')
 const[config,setConfig]=useState<any>(null)
 const[areas,setAreas]=useState<TableArea[]>([])
 const[tables,setTables]=useState<TableRow[]>([])
 const[newArea,setNewArea]=useState('')
 const[newTable,setNewTable]=useState({area_id:'',name:'',capacity:4})
 const[tableCreateMode,setTableCreateMode]=useState<'individual'|'automatic'>('individual')
 const[bulkTable,setBulkTable]=useState<BulkTableDraft>({area_id:'',base_name:'Mesa',suffix_type:'numeric',quantity:10,capacity:4})
 const[loading,setLoading]=useState(false),[busy,setBusy]=useState(false),[err,setErr]=useState(''),[ok,setOk]=useState(''),[qrTable,setQrTable]=useState<TableRow|null>(null),[qrDesigner,setQrDesigner]=useState(false),[qrStyle,setQrStyle]=useState<QRStyle>(defaultQRStyle),[tableQrStyle,setTableQrStyle]=useState<QRStyle>(defaultQRStyle),[tableQrDesigner,setTableQrDesigner]=useState(false)
 const capabilities=useMemo(()=>resolveBusinessCapabilities(config),[config])
 const enabled=!!config?.dine_in_enabled&&capabilities.supportsDineIn
 const activeAreas=useMemo(()=>areas.filter(x=>x.is_active!==false),[areas])
 const activeTables=useMemo(()=>tables.filter(x=>x.is_active!==false),[tables])
 const bulkPreview=useMemo(()=>{const quantity=Math.min(500,Math.max(0,Number(bulkTable.quantity)||0));return Array.from({length:Math.min(quantity,8)},(_,i)=>generatedTableName(bulkTable.base_name,bulkTable.suffix_type,i+1))},[bulkTable.base_name,bulkTable.suffix_type,bulkTable.quantity])
 const load=async()=>{
  if(!store){setConfig(null);setAreas([]);setTables([]);setQrStyle(defaultQRStyle);return}
  setLoading(true);setErr('')
  try{
   const[cfg,areaRows,tableRows,style]=await Promise.all([api<any>(`/stores/${store}/settings`),api<TableArea[]>(`/table-areas?store_id=${store}`),api<TableRow[]>(`/tables?store_id=${store}`),api<QRStyle>(`/qr-style?store_id=${store}`)])
   setConfig(cfg);setAreas(areaRows||[]);setTables(tableRows||[]);setQrStyle({...defaultQRStyle,...style})
   const first=(areaRows||[]).find(x=>x.is_active!==false)
   setNewTable(v=>({...v,area_id:(areaRows||[]).some(x=>x.id===v.area_id&&x.is_active!==false)?v.area_id:first?.id||''}))
   setBulkTable(v=>({...v,area_id:(areaRows||[]).some(x=>x.id===v.area_id&&x.is_active!==false)?v.area_id:first?.id||''}))
  }catch(e:any){setErr(e.message)}finally{setLoading(false)}
 }
 useEffect(()=>{void load()},[store])
 const reloadManagement=async()=>{
  const[areaRows,tableRows]=await Promise.all([api<TableArea[]>(`/table-areas?store_id=${store}`),api<TableRow[]>(`/tables?store_id=${store}`)])
  setAreas(areaRows||[]);setTables(tableRows||[])
  const first=(areaRows||[]).find(x=>x.is_active!==false)
  setNewTable(v=>({...v,area_id:(areaRows||[]).some(x=>x.id===v.area_id&&x.is_active!==false)?v.area_id:first?.id||''}))
  setBulkTable(v=>({...v,area_id:(areaRows||[]).some(x=>x.id===v.area_id&&x.is_active!==false)?v.area_id:first?.id||''}))
 }
 const addArea=async()=>{if(!newArea.trim()||!enabled)return;setBusy(true);setErr('');setOk('');try{await api('/table-areas',{method:'POST',body:JSON.stringify({store_id:store,name:newArea.trim()})});setNewArea('');await reloadManagement();setOk('Área agregada correctamente.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const saveArea=async(row:TableArea)=>{setBusy(true);setErr('');setOk('');try{await api(`/table-areas/${row.id}`,{method:'PUT',body:JSON.stringify({store_id:store,name:row.name.trim(),sort_order:Number(row.sort_order)||100,is_active:row.is_active!==false})});await reloadManagement();setOk('Área actualizada.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const archiveArea=async(row:TableArea)=>{if(!confirm(`¿Archivar el área ${row.name}?`))return;setBusy(true);setErr('');setOk('');try{await api(`/table-areas/${row.id}`,{method:'DELETE'});await reloadManagement();setOk('Área archivada.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const addTable=async()=>{if(!newTable.area_id||!newTable.name.trim()||!enabled)return;setBusy(true);setErr('');setOk('');try{await api('/tables',{method:'POST',body:JSON.stringify({store_id:store,area_id:newTable.area_id,name:newTable.name.trim(),capacity:Math.max(1,Number(newTable.capacity)||4)})});setNewTable(v=>({...v,name:'',capacity:4}));await reloadManagement();setOk('Mesa agregada correctamente.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const addTablesBulk=async()=>{const quantity=Number(bulkTable.quantity)||0;if(!bulkTable.area_id||!bulkTable.base_name.trim()||quantity<1||quantity>500||!enabled)return;setBusy(true);setErr('');setOk('');try{const out=await api<{count:number}>('/tables/bulk',{method:'POST',body:JSON.stringify({store_id:store,area_id:bulkTable.area_id,base_name:bulkTable.base_name.trim(),suffix_type:bulkTable.suffix_type,quantity,capacity:Math.max(1,Number(bulkTable.capacity)||4)})});await reloadManagement();setOk(`${out.count||quantity} mesas creadas correctamente.`)}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const saveTable=async(row:TableRow)=>{setBusy(true);setErr('');setOk('');try{await api(`/tables/${row.id}`,{method:'PUT',body:JSON.stringify({store_id:store,area_id:row.area_id,name:row.name.trim(),capacity:Math.max(1,Number(row.capacity)||1),sort_order:Number(row.sort_order)||100,is_active:row.is_active!==false})});await reloadManagement();setOk('Mesa actualizada.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const archiveTable=async(row:TableRow)=>{if(!confirm(`¿Archivar ${row.name}?`))return;setBusy(true);setErr('');setOk('');try{await api(`/tables/${row.id}`,{method:'DELETE'});await reloadManagement();setOk('Mesa archivada.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const saveDuration=async(value:number)=>{if(!config)return;const next={...config,reservation_duration_minutes:value};setConfig(next);setBusy(true);setErr('');setOk('');try{await api(`/stores/${store}/settings`,{method:'PUT',body:JSON.stringify(next)});if(typeof window!=='undefined')window.dispatchEvent(new CustomEvent('wamercio:store-settings-changed',{detail:{store_id:store}}));setOk('Duración de reserva actualizada.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const publicUrl=config?.slug?`https://${config.slug}.${process.env.NEXT_PUBLIC_TENANT_ROOT_DOMAIN||'ltd.do'}`:''
 const tableUrl=(row:TableRow|null)=>row&&publicUrl?`${publicUrl}/?table=${encodeURIComponent(row.id)}`:''
 const downloadQR=()=>{if(!qrTable||typeof document==='undefined')return;const node=document.getElementById('wamercio-table-qr');if(!node)return;const source=new XMLSerializer().serializeToString(node);const blob=new Blob([source],{type:'image/svg+xml;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${String(config?.slug||'wamercio')}-${qrTable.name.toLowerCase().replace(/[^a-z0-9]+/g,'-')}.svg`;a.click();URL.revokeObjectURL(url)}
 const saveQRStyle=async()=>{setBusy(true);setErr('');setOk('');try{const saved=await api<QRStyle>('/qr-style',{method:'PUT',body:JSON.stringify({...qrStyle,store_id:store})});setQrStyle({...defaultQRStyle,...saved});setQrDesigner(false);setOk('Diseño QR actualizado.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const openTableQR=async(row:TableRow)=>{setErr('');setQrTable(row);setTableQrDesigner(false);setTableQrStyle({...defaultQRStyle,...qrStyle,is_override:false,table_id:row.id});try{const style=await api<QRStyle>(`/qr-style?store_id=${store}&table_id=${row.id}`);setTableQrStyle({...defaultQRStyle,...style,table_id:row.id})}catch(e:any){setErr(e.message)}}
 const saveTableQRStyle=async()=>{if(!qrTable)return;setBusy(true);setErr('');setOk('');try{const saved=await api<QRStyle>('/qr-style',{method:'PUT',body:JSON.stringify({...tableQrStyle,store_id:store,table_id:qrTable.id})});setTableQrStyle({...defaultQRStyle,...saved,table_id:qrTable.id});setTableQrDesigner(false);setOk(`Diseño QR personalizado para ${qrTable.name}.`)}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const resetTableQRStyle=async()=>{if(!qrTable)return;setBusy(true);setErr('');setOk('');try{const saved=await api<QRStyle>(`/qr-style/table/${qrTable.id}?store_id=${store}`,{method:'DELETE'});setTableQrStyle({...defaultQRStyle,...saved,table_id:qrTable.id,is_override:false});setTableQrDesigner(false);setOk(`${qrTable.name} vuelve a usar el diseño general.`)}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 return <StoreShell title="Gestión de mesas" subtitle="Organiza áreas, mesas, capacidad y duración de las reservas" context={<StoreSelector value={store} onChange={setStore}/>} actions={<button disabled={!store} className="btn-secondary" onClick={()=>setQrDesigner(true)}><Palette className="h-4 w-4"/>Diseñar QR</button>}>
  {!store?<PageEmpty title="Selecciona un negocio" detail="Elige el negocio donde administrarás las mesas."/>:loading?<Loading/>:<div className="space-y-5">
   {err&&<Alert text={err}/>} {ok&&<Alert text={ok} type="success"/>}
   {!enabled?<section className="card p-6"><div className="flex items-start gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-amber-50 text-amber-600"><UtensilsCrossed className="h-5 w-5"/></span><div><h2 className="text-lg font-semibold">Mesas y reservas está desactivado</h2><p className="mt-1 text-sm leading-6 text-[#8d92aa]">Activa <strong>Mesas y reservas</strong> en Configuración → Ventas y entrega para administrar áreas y mesas.</p><a className="btn-secondary mt-4 inline-flex" href="/settings/store?tab=sales">Ir a Ventas y entrega</a></div></div></section>:<>
    <section className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_300px]">
     <div className="card p-5 sm:p-6">
      <div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-brand-50 text-brand-600"><UtensilsCrossed className="h-5 w-5"/></span><div><p className="section-kicker">Organización</p><h2 className="text-xl font-semibold">Áreas del negocio</h2><p className="mt-1 text-sm text-[#8d92aa]">Crea cada área una sola vez y organiza dentro todas sus mesas.</p></div></div>
      <div className="mt-5 grid gap-3 sm:grid-cols-[1fr_auto]"><div><label className="label">Nombre del área</label><input className="field" placeholder="Ej.: Salón principal, Terraza, VIP" value={newArea} onChange={e=>setNewArea(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();void addArea()}}}/></div><div className="flex items-end"><button type="button" disabled={busy||!newArea.trim()} onClick={addArea} className="btn-primary w-full justify-center"><Plus className="h-4 w-4"/>Agregar área</button></div></div>
      <div className="mt-5 grid gap-3 md:grid-cols-2">{activeAreas.length?activeAreas.map(area=><article key={area.id} className="rounded-2xl border border-[#e7ebe9] bg-[#fbfcfc] p-4"><div className="flex items-start gap-3"><div className="min-w-0 flex-1"><label className="label">Área</label><input className="field" value={area.name} onChange={e=>setAreas(v=>v.map(x=>x.id===area.id?{...x,name:e.target.value}:x))}/><div className="mt-2 flex flex-wrap gap-2 text-[11px] text-[#7f8799]"><span className="rounded-full bg-white px-2.5 py-1">{area.table_count||0} mesas</span><span className="rounded-full bg-white px-2.5 py-1">{area.capacity||0} personas</span></div></div><div className="flex gap-1.5 pt-6"><button type="button" disabled={busy||!area.name.trim()} onClick={()=>saveArea(area)} className="btn-secondary px-2.5" title="Guardar área"><Save className="h-4 w-4"/></button><button type="button" disabled={busy||(area.table_count||0)>0} onClick={()=>archiveArea(area)} className="btn-danger px-2.5" title={(area.table_count||0)>0?'Mueve o archiva sus mesas primero':'Archivar área'}><Trash2 className="h-4 w-4"/></button></div></div></article>):<div className="md:col-span-2 rounded-2xl border border-dashed border-[#dfe4e6] p-6 text-center text-sm text-[#8d92aa]">Crea tu primera área para comenzar a organizar las mesas.</div>}</div>
     </div>
     <aside className="card p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-brand-50 text-brand-600"><Clock3 className="h-5 w-5"/></span><div><p className="section-kicker">Reservas</p><h3 className="font-semibold">Duración de la reserva</h3></div></div><p className="mt-3 text-xs leading-5 text-[#8d92aa]">Define el tiempo estándar que ocupará una mesa para calcular la disponibilidad.</p><select className="field mt-4" value={config?.reservation_duration_minutes||90} disabled={busy} onChange={e=>void saveDuration(Number(e.target.value))}><option value={60}>60 minutos</option><option value={90}>90 minutos</option><option value={120}>120 minutos</option><option value={180}>180 minutos</option></select></aside>
    </section>

    <section className="card p-5 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="section-kicker">Nueva mesa</p><h2 className="mt-1 text-lg font-semibold">Agregar mesas</h2><p className="mt-1 text-sm text-[#8d92aa]">Crea una mesa individual o genera automáticamente un lote completo dentro del área seleccionada.</p></div><span className="inline-flex items-center gap-2 rounded-full bg-brand-50 px-3 py-1.5 text-xs font-semibold text-brand-700"><UsersRound className="h-4 w-4"/>{activeTables.length} mesas · {activeTables.reduce((sum,row)=>sum+Number(row.capacity||0),0)} personas</span></div>
     <div className="mt-5 inline-flex rounded-2xl bg-[#f4f6f7] p-1"><button type="button" onClick={()=>setTableCreateMode('individual')} className={`rounded-xl px-4 py-2 text-sm font-semibold transition ${tableCreateMode==='individual'?'bg-white text-brand-700 shadow-sm':'text-[#7d8496] hover:text-ink-900'}`}>Individual</button><button type="button" onClick={()=>setTableCreateMode('automatic')} className={`rounded-xl px-4 py-2 text-sm font-semibold transition ${tableCreateMode==='automatic'?'bg-white text-brand-700 shadow-sm':'text-[#7d8496] hover:text-ink-900'}`}>Creación automática</button></div>
     {tableCreateMode==='individual'?<div className="mt-5 grid gap-3 md:grid-cols-[220px_minmax(0,1fr)_150px_auto]"><div><label className="label">Área *</label><select className="field" value={newTable.area_id} onChange={e=>{const area_id=e.target.value;setNewTable({...newTable,area_id});setBulkTable({...bulkTable,area_id})}}><option value="">Selecciona un área</option>{activeAreas.map(area=><option key={area.id} value={area.id}>{area.name}</option>)}</select></div><div><label className="label">Nombre de la mesa</label><input className="field" placeholder="Ej.: Mesa 1" value={newTable.name} onChange={e=>setNewTable({...newTable,name:e.target.value})}/></div><div><label className="label">Capacidad</label><input className="field" type="number" min="1" max="50" value={newTable.capacity} onChange={e=>setNewTable({...newTable,capacity:Number(e.target.value)})}/></div><div className="flex items-end"><button type="button" disabled={busy||!newTable.area_id||!newTable.name.trim()} onClick={addTable} className="btn-primary w-full justify-center"><Plus className="h-4 w-4"/>Agregar mesa</button></div></div>:<div className="mt-5 space-y-4"><div className="grid gap-3 lg:grid-cols-[220px_minmax(180px,1fr)_180px_140px_140px]"><div><label className="label">Área *</label><select className="field" value={bulkTable.area_id} onChange={e=>{const area_id=e.target.value;setBulkTable({...bulkTable,area_id});setNewTable({...newTable,area_id})}}><option value="">Selecciona un área</option>{activeAreas.map(area=><option key={area.id} value={area.id}>{area.name}</option>)}</select></div><div><label className="label">Nombre base</label><input className="field" placeholder="Mesa" value={bulkTable.base_name} maxLength={70} onChange={e=>setBulkTable({...bulkTable,base_name:e.target.value})}/></div><div><label className="label">Sufijo</label><select className="field" value={bulkTable.suffix_type} onChange={e=>setBulkTable({...bulkTable,suffix_type:e.target.value as BulkTableDraft['suffix_type']})}><option value="numeric">Numérico · 1, 2, 3</option><option value="alphabetic">Alfabético · A, B, C</option><option value="mixed">Mixto · A1, A2, A3</option></select></div><div><label className="label">Cantidad</label><input className="field" type="number" min="1" max="500" value={bulkTable.quantity} onChange={e=>setBulkTable({...bulkTable,quantity:Number(e.target.value)})}/></div><div><label className="label">Capacidad</label><input className="field" type="number" min="1" max="50" value={bulkTable.capacity} onChange={e=>setBulkTable({...bulkTable,capacity:Number(e.target.value)})}/></div></div><div className="flex flex-col gap-4 rounded-2xl border border-[#e7ebe9] bg-[#fbfcfc] p-4 lg:flex-row lg:items-center lg:justify-between"><div className="min-w-0"><p className="section-kicker">Vista previa</p><div className="mt-2 flex flex-wrap gap-2">{bulkPreview.length?bulkPreview.map(name=><span key={name} className="rounded-full border border-[#e6eae8] bg-white px-3 py-1.5 text-xs font-semibold text-[#61697a]">{name}</span>):<span className="text-sm text-[#8d92aa]">Indica una cantidad válida para ver los nombres.</span>}{Number(bulkTable.quantity)>8&&<span className="rounded-full bg-brand-50 px-3 py-1.5 text-xs font-semibold text-brand-700">+ {Math.min(500,Number(bulkTable.quantity))-8} más</span>}</div><p className="mt-2 text-xs text-[#8d92aa]">Máximo 500 mesas por operación. Si algún nombre ya existe, no se crea ninguna mesa del lote.</p></div><button type="button" disabled={busy||!bulkTable.area_id||!bulkTable.base_name.trim()||Number(bulkTable.quantity)<1||Number(bulkTable.quantity)>500} onClick={addTablesBulk} className="btn-primary shrink-0 justify-center"><Plus className="h-4 w-4"/>Crear {Math.max(0,Math.min(500,Number(bulkTable.quantity)||0))} mesas</button></div></div>}
    </section>

    <section className="card overflow-hidden"><div className="border-b border-[#edf0f5] p-5"><p className="section-kicker">Mesas activas</p><h2 className="mt-1 text-lg font-semibold">Administrar mesas por área</h2></div>
     <div className="divide-y divide-[#edf0f5]">{activeTables.length?activeAreas.map(area=>{const rows=activeTables.filter(row=>row.area_id===area.id);if(!rows.length)return null;return <div key={area.id} className="p-4 sm:p-5"><div className="mb-3 flex items-center justify-between gap-3"><div><h3 className="font-semibold text-ink-900">{area.name}</h3><p className="text-xs text-[#8d92aa]">{rows.length} {rows.length===1?'mesa':'mesas'} · {rows.reduce((sum,row)=>sum+Number(row.capacity||0),0)} personas</p></div></div><div className="space-y-2">{rows.map(row=><div key={row.id} className="grid items-end gap-3 rounded-2xl border border-[#edf0f5] bg-[#fbfcfc] p-3 md:grid-cols-[200px_minmax(0,1fr)_140px_auto]"><div><label className="label">Área</label><select className="field" value={row.area_id} onChange={e=>setTables(v=>v.map(x=>x.id===row.id?{...x,area_id:e.target.value,area_name:activeAreas.find(a=>a.id===e.target.value)?.name||x.area_name}:x))}>{activeAreas.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></div><div><label className="label">Mesa</label><input className="field" value={row.name} onChange={e=>setTables(v=>v.map(x=>x.id===row.id?{...x,name:e.target.value}:x))}/></div><div><label className="label">Capacidad</label><input className="field" type="number" min="1" max="50" value={row.capacity} onChange={e=>setTables(v=>v.map(x=>x.id===row.id?{...x,capacity:Number(e.target.value)}:x))}/></div><div className="flex gap-2"><button type="button" className="btn-secondary px-3" onClick={()=>void openTableQR(row)} title="QR de la mesa"><QrCode className="h-4 w-4"/></button><button type="button" disabled={busy||!row.name.trim()} className="btn-secondary px-3" onClick={()=>saveTable(row)}><Save className="h-4 w-4"/>Guardar</button><button type="button" disabled={busy} className="btn-danger px-3" onClick={()=>archiveTable(row)} title="Archivar mesa"><Trash2 className="h-4 w-4"/></button></div></div>)}</div></div>}):<div className="p-10 text-center text-sm text-[#8d92aa]">Agrega tu primera mesa para aceptar reservas.</div>}</div>
    </section>
   </>}
  </div>}
  <Modal open={!!qrTable} onClose={()=>{setQrTable(null);setTableQrDesigner(false)}} title={qrTable?`QR · ${qrTable.name}`:'QR de mesa'} subtitle="Al escanearlo, el cliente abrirá el catálogo con esta mesa preseleccionada.">
   {qrTable&&<div><div className="text-center"><div className={`mx-auto inline-grid place-items-center p-5 ${tableQrStyle.frame_style==='card'?'rounded-3xl shadow-lg':tableQrStyle.frame_style==='minimal'?'':'rounded-3xl border border-[#e9ebf2]'}`} style={{background:tableQrStyle.background}}><QRCodeSVG id="wamercio-table-qr" value={tableUrl(qrTable)} size={220} level="M" includeMargin fgColor={tableQrStyle.foreground} bgColor={tableQrStyle.background}/><p className="mt-3 text-sm font-semibold" style={{color:tableQrStyle.foreground}}>{tableQrStyle.label}</p>{tableQrStyle.show_logo&&config?.logo_url&&<img src={config.logo_url} alt="Logo" className="mt-3 h-9 w-9 rounded-lg object-cover"/>}</div><div className="mt-4"><p className="text-sm font-semibold text-ink-900">{qrTable.area_name} · {qrTable.name}</p><p className="mt-1 text-xs text-[#8d92aa]">Capacidad: {qrTable.capacity} personas · {tableQrStyle.is_override?'Diseño personalizado':'Diseño general'}</p></div><div className="mt-5 flex items-center gap-2 rounded-2xl bg-[#f7f8fa] p-2"><input className="min-w-0 flex-1 bg-transparent px-2 text-sm text-slate-600 outline-none" readOnly value={tableUrl(qrTable)}/><button className="btn-secondary px-3" onClick={()=>navigator.clipboard.writeText(tableUrl(qrTable))}><Copy className="h-4 w-4"/>Copiar</button></div><div className="mt-3 grid gap-2 sm:grid-cols-2"><button type="button" className="btn-secondary justify-center" onClick={downloadQR}><Download className="h-4 w-4"/>Descargar SVG</button><a className="btn-primary justify-center" href={tableUrl(qrTable)} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4"/>Probar QR</a></div><div className="mt-3 grid gap-2 sm:grid-cols-2"><button type="button" className="btn-secondary justify-center" onClick={()=>setTableQrDesigner(v=>!v)}><Palette className="h-4 w-4"/>Personalizar esta mesa</button><button type="button" disabled={busy||!tableQrStyle.is_override} className="btn-secondary justify-center" onClick={()=>void resetTableQRStyle()}>Usar diseño general</button></div></div>{tableQrDesigner&&<div className="mt-5 space-y-4 rounded-3xl border border-[#edf0f4] bg-[#fbfcfc] p-4 text-left"><div><p className="section-kicker">Diseño individual</p><h3 className="mt-1 font-semibold">Personalizar {qrTable.name}</h3><p className="mt-1 text-xs leading-5 text-[#8d92aa]">Este estilo solo aplica a esta mesa. Puedes restablecerlo al diseño general cuando quieras.</p></div><div className="grid grid-cols-2 gap-3"><div><label className="label">Color QR</label><input className="field h-11 p-1" type="color" value={tableQrStyle.foreground} onChange={e=>setTableQrStyle({...tableQrStyle,foreground:e.target.value.toUpperCase()})}/></div><div><label className="label">Fondo</label><input className="field h-11 p-1" type="color" value={tableQrStyle.background} onChange={e=>setTableQrStyle({...tableQrStyle,background:e.target.value.toUpperCase()})}/></div></div><div><label className="label">Texto</label><input className="field" value={tableQrStyle.label} onChange={e=>setTableQrStyle({...tableQrStyle,label:e.target.value})}/></div><div><label className="label">Marco</label><select className="field" value={tableQrStyle.frame_style} onChange={e=>setTableQrStyle({...tableQrStyle,frame_style:e.target.value as QRStyle['frame_style']})}><option value="rounded">Redondeado</option><option value="minimal">Minimalista</option><option value="card">Tarjeta</option></select></div><label className="flex items-center gap-3 rounded-2xl border border-[#edf0f4] bg-white p-3 text-sm"><input type="checkbox" checked={tableQrStyle.show_logo} onChange={e=>setTableQrStyle({...tableQrStyle,show_logo:e.target.checked})}/><span><strong className="block">Mostrar logo</strong><span className="text-xs text-[#969caf]">Utiliza el logo actual del negocio debajo del código.</span></span></label><button disabled={busy} className="btn-primary w-full justify-center" onClick={()=>void saveTableQRStyle()}><Save className="h-4 w-4"/>Guardar para esta mesa</button></div>}</div>}
  </Modal>
  <Modal open={qrDesigner} onClose={()=>setQrDesigner(false)} title="Diseñar QR" subtitle="Personaliza todos los QR de mesa sin cambiar el enlace ni la lógica de pedidos.">
   <div className="grid gap-5 sm:grid-cols-[220px_minmax(0,1fr)]"><div className={`grid place-items-center p-5 ${qrStyle.frame_style==='card'?'rounded-3xl shadow-lg':qrStyle.frame_style==='minimal'?'':'rounded-3xl border border-[#e8ebf0]'}`} style={{background:qrStyle.background}}><QRCodeSVG value={publicUrl||'https://wamercio.com'} size={180} level="M" includeMargin fgColor={qrStyle.foreground} bgColor={qrStyle.background}/><p className="mt-3 text-center text-sm font-semibold" style={{color:qrStyle.foreground}}>{qrStyle.label}</p>{qrStyle.show_logo&&config?.logo_url&&<img src={config.logo_url} alt="Logo" className="mt-3 h-9 w-9 rounded-lg object-cover"/>}</div><div className="space-y-4"><div className="grid grid-cols-2 gap-3"><div><label className="label">Color QR</label><input className="field h-11 p-1" type="color" value={qrStyle.foreground} onChange={e=>setQrStyle({...qrStyle,foreground:e.target.value.toUpperCase()})}/></div><div><label className="label">Fondo</label><input className="field h-11 p-1" type="color" value={qrStyle.background} onChange={e=>setQrStyle({...qrStyle,background:e.target.value.toUpperCase()})}/></div></div><div><label className="label">Texto</label><input className="field" value={qrStyle.label} onChange={e=>setQrStyle({...qrStyle,label:e.target.value})}/></div><div><label className="label">Marco</label><select className="field" value={qrStyle.frame_style} onChange={e=>setQrStyle({...qrStyle,frame_style:e.target.value as QRStyle['frame_style']})}><option value="rounded">Redondeado</option><option value="minimal">Minimalista</option><option value="card">Tarjeta</option></select></div><label className="flex items-center gap-3 rounded-2xl border border-[#edf0f4] p-3 text-sm"><input type="checkbox" checked={qrStyle.show_logo} onChange={e=>setQrStyle({...qrStyle,show_logo:e.target.checked})}/><span><strong className="block">Mostrar logo</strong><span className="text-xs text-[#969caf]">Lo muestra debajo del código cuando la tienda tiene logo.</span></span></label><button disabled={busy} className="btn-primary w-full justify-center" onClick={()=>void saveQRStyle()}><Save className="h-4 w-4"/>Guardar diseño</button></div></div>
  </Modal>
 </StoreShell>
}
