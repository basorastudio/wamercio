'use client'
import {useEffect,useState} from 'react'
import AdminShell from '@/components/store-shell'
import {api,dateTime} from '@/lib/api'
import {Alert,Loading} from '@/components/ui'
import {UserRound,ShieldCheck,Phone,KeyRound,Save,MessageCircleMore} from 'lucide-react'

export default function Profile(){
 const[me,setMe]=useState<any>(null),[form,setForm]=useState<any>(null),[pin,setPin]=useState({current_pin:'',new_pin:'',confirm:''}),[err,setErr]=useState(''),[ok,setOk]=useState(''),[busy,setBusy]=useState(false)
 const load=()=>api('/me').then((x:any)=>{setMe(x);setForm({name:x.name,phone:x.phone||''})})
 useEffect(()=>{load()},[])
 const save=async(e:React.FormEvent)=>{e.preventDefault();setErr('');setOk('');setBusy(true);try{await api('/me',{method:'PATCH',body:JSON.stringify(form)});await load();setOk('Perfil actualizado correctamente.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 const change=async(e:React.FormEvent)=>{e.preventDefault();setErr('');setOk('');if(!/^\d{4}$/.test(pin.new_pin)){setErr('El PIN nuevo debe tener exactamente 4 dígitos.');return}if(pin.new_pin!==pin.confirm){setErr('Los PIN nuevos no coinciden.');return}setBusy(true);try{await api('/me/pin',{method:'POST',body:JSON.stringify({current_pin:pin.current_pin,new_pin:pin.new_pin})});setPin({current_pin:'',new_pin:'',confirm:''});setOk('PIN actualizado correctamente.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}
 return <AdminShell title="Mi cuenta" subtitle="WhatsApp, perfil y PIN de acceso">{!me||!form?<Loading/>:<div>{err&&<Alert text={err}/>} {ok&&<Alert text={ok} type="success"/>}<div className="grid gap-5 xl:grid-cols-[1fr_420px]"><form onSubmit={save} className="card p-5 sm:p-6"><div className="flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-amber-50 text-amber-600"><UserRound className="h-7 w-7"/></div><div><h2 className="text-xl font-bold">{me.name}</h2><p className="text-sm text-slate-500">Cuenta comercial WAMERCIO</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><label className="label">Nombre</label><input className="field" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label className="label">WhatsApp de acceso</label><div className="relative"><MessageCircleMore className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-emerald-500"/><input className="field pl-9" inputMode="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><p className="mt-1 text-xs text-slate-400">Este número funciona como tu usuario.</p></div>{me.email&&<div className="sm:col-span-2"><label className="label">Correo de contacto</label><div className="field bg-slate-50 text-slate-500">{me.email}</div></div>}</div><div className="mt-6 flex flex-col gap-3 border-t border-slate-100 pt-5 sm:flex-row sm:items-center sm:justify-between"><p className="text-xs text-slate-400">Cuenta creada: {dateTime(me.created_at)}</p><button disabled={busy} className="btn-primary bg-amber-400 text-slate-950 hover:bg-amber-500"><Save className="h-4 w-4"/>Guardar cambios</button></div></form><form onSubmit={change} className="card p-5 sm:p-6"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-50 text-emerald-600"><ShieldCheck/></div><h3 className="mt-4 text-lg font-bold">PIN de acceso</h3><p className="mt-1 text-sm leading-6 text-slate-500">Tu PIN tiene solo 4 dígitos para que puedas entrar rápido desde el móvil.</p><div className="mt-5 space-y-4"><div><label className="label">PIN actual</label><input className="field text-center text-lg font-bold tracking-[.35em]" inputMode="numeric" type="password" maxLength={4} value={pin.current_pin} onChange={e=>setPin({...pin,current_pin:e.target.value.replace(/\D/g,'').slice(0,4)})} required/></div><div className="grid grid-cols-2 gap-3"><div><label className="label">Nuevo PIN</label><input className="field text-center text-lg font-bold tracking-[.35em]" inputMode="numeric" type="password" maxLength={4} value={pin.new_pin} onChange={e=>setPin({...pin,new_pin:e.target.value.replace(/\D/g,'').slice(0,4)})} required/></div><div><label className="label">Confirmar</label><input className="field text-center text-lg font-bold tracking-[.35em]" inputMode="numeric" type="password" maxLength={4} value={pin.confirm} onChange={e=>setPin({...pin,confirm:e.target.value.replace(/\D/g,'').slice(0,4)})} required/></div></div></div><button disabled={busy} className="btn-secondary mt-5 w-full"><KeyRound className="h-4 w-4"/>Cambiar PIN</button></form></div></div>}</AdminShell>
}
